---
deprecations:
  - |
    The ``osc_lib.version`` module and ``osc_lib.__version__`` attribute have
    been deprecated for removal. Prefer use of ``importlib.metadata`` to
    inspect version information for installed packages.
